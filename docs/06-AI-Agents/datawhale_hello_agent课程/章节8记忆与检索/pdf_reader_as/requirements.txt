# ---- 运行时直接依赖 ----
# Web UI
gradio>=6.0.0

# LLM（OpenAI 兼容接口，本地 Ollama / 云端均可）
openai>=1.0.0

# 向量数据库客户端（Qdrant）
qdrant-client>=1.9.0

# 文档解析（markitdown 需 [pdf] 依赖拉取 pdfplumber/pypdfium2 等）
markitdown[pdf]>=0.1.0
pdfplumber>=0.10.0
pypdfium2>=4.0.0

# 文本语言检测
langdetect>=1.0.9

# 本地嵌入模型（sentence-transformers；会随之安装 torch）
sentence-transformers>=3.0.0
torch>=2.0.0

# 图数据库（语义记忆可选；不启用可注释掉）
neo4j>=5.0.0

# NLP 实体提取（可选；需额外下载语言模型，如 python -m spacy download zh_core_web_sm）
spacy>=3.0.0

# 基础
pydantic>=2.0.0
python-dotenv>=1.0.0
numpy>=1.24.0
requests>=2.28.0

# 嵌入兜底（TF-IDF）
scikit-learn>=1.0.0
